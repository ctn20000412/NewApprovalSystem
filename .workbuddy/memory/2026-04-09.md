# 2026-04-09 工作日志

## MySQL 数据库初始化脚本完成

重写了 inventory-system 的数据库脚本，文件位于 `e:/code/workbuddy/System/inventory-system/database/`：

| 文件 | 内容 |
|------|------|
| `schema.sql` | 建库 + 7张表 + 15个索引（新增，与实体类严格对齐） |
| `data.sql` | 用户(4) + 分类(13) + 产品(44) + 库存初始日志(15)（重写）|
| `request_data.sql` | 申请单(6) + 明细(17) + 订单(3) + 出入库日志(5)（重写）|
| `setup.sql` | 一键导入入口，SOURCE 三个子脚本 + 验证查询（新增）|

**关键修正：**
- ChangeType 枚举从 `PURCHASE` 改为正确的 `IN / OUT / ADJUST`
- BCrypt 密码哈希使用 Python bcrypt 实测验证 `$2b$10$...`（密码: 123456）
- 订单状态覆盖 `PENDING / CONFIRMED / COMPLETED / CANCELLED`
- RequestStatus 覆盖 `PENDING / APPROVED / REJECTED / COMPLETED`

## Order 实体修复

修复了 `ApiOrderController.toOrderResponse()` 调用 `order.getCompletedAt()` 报错的问题：

| 文件 | 修改 |
|------|------|
| `entity/Order.java` | 添加 `completedAt` 字段 |
| `database/schema.sql` | 添加 `completed_at` 列 |
| `service/OrderService.java` | `completeOrder()` 方法设置 `completedAt = LocalDateTime.now()` |

## 用户使用指南

创建了完整的使用文档：`e:/code/workbuddy/System/用户使用指南.md`
- 系统简介和功能模块
- 环境要求和快速启动
- 分步详细启动说明
- 项目结构和 API 接口
- 配置说明和常见问题

## 前端界面美化

全面升级 UI 设计，采用紫色主题（Purple Theme）：

| 页面/组件 | 改进内容 |
|-----------|----------|
| `index.css` | 全新设计系统：渐变背景、卡片样式、按钮、表单、表格、动画 |
| `tailwind.config.js` | 紫色主题配色、自定义阴影、动画关键帧 |
| `Login.tsx` | 左右分栏布局、装饰背景、快速填充账号按钮 |
| `Dashboard.tsx` | 统计卡片、快捷操作卡片、最近请求表格、用户信息卡片 |
| `ProductList.tsx` | 网格/列表双视图、渐变图标、状态标签、悬停效果 |
| `OrderList.tsx` | 统计概览、状态图标、金额格式化 |
| `Layout.tsx` | 新侧边栏设计、用户卡片、顶部导航栏 |

**设计特点：**
- 紫色渐变主题（Primary: #7C3AED）
- 橙色强调色（Accent: #F97316）
- Plus Jakarta Sans 字体
- 毛玻璃效果（backdrop-blur）
- 平滑过渡动画
- 卡片悬停效果
