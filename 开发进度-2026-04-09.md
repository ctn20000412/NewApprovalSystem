# 开发进度日志

## 日期
2026-04-09

## 今日完成

### 1. 前端基础运行问题修复
- 清理了 `inventory-frontend/src` 下遗留的 `.js/.jsx` 冗余文件，避免旧文件覆盖 `.ts/.tsx` 逻辑。
- 修复了前端入口和页面模块引用，统一显式指向 TypeScript 文件。
- 修复了前端白页问题，完成了前端构建校验，当前前端代码可以正常打包。

### 2. 前后端登录链路修复
- 重新梳理了前端登录请求路径，前端现在使用 `/auth/login` 和 `/auth/logout`。
- 修复了 Vite 代理配置，避免将 `/auth/login` 错误改写成 `/login`。
- 修复了前后端登录路由冲突和重定向死循环问题。
- 后端新增数据库认证实现，Spring Security 现在会从数据库读取用户信息进行登录校验。

### 3. 后端旧页面代码清理
- 删除了 8080 端口原有的 Thymeleaf 页面控制器和模板。
- 后端现在只保留：
  - React 前端跳转入口
  - Spring Security 认证
  - `/api/**` 接口
- 移除了 Thymeleaf 相关依赖和配置，减少冗余代码。

### 4. React 前端与现有后端接口对齐
- 已完成认证、产品、取货申请、订单四个模块的接口对齐。
- 修复了产品表单字段与后端字段不一致的问题，前端现已按后端实际字段提交：
  - `spec`
  - `costPrice`
  - `retailPrice`
  - `minStock`
- 修复了订单状态与后端不一致的问题，前端已按后端 `CONFIRMED` 状态流处理。

## 当前已实现并可联通的模块
- 登录认证
- 仪表盘
- 产品管理
- 取货申请管理
- 订单管理

## 当前前端仍待开发的模块
- 货仓管理页面
- 用户管理页面
- 统计报表页面
- 我的业绩页面
- 产品详情中的库存流水展示

## 当前后端仍待补充接口的模块
- `/api/warehouse/**`
- `/api/users/**`
- `/api/reports/**`

说明：
- 报表相关业务逻辑在服务层已经存在，但尚未开放为 React 可调用的 API。
- 用户管理相关业务逻辑在服务层已经存在，但尚未开放为 React 可调用的 API。
- 货仓和库存流水目前前端已有预留调用，但后端未提供对应接口。

## 当前状态判断
- 系统主业务链路已经基本成型：
  - 登录 -> 取货申请 -> 审批 -> 创建订单 -> 扣减库存
- 目前项目处于“主链路可用，管理增强模块待补齐”的阶段。

## 下一步建议
优先开发货仓模块，原因如下：
- 可以补齐库存流水接口
- 可以完善产品详情页
- 可以为后续报表模块提供更完整的数据展示基础

建议开发顺序：
1. 货仓模块
2. 用户管理模块
3. 报表模块
4. 页面细节优化与联调回归
